# Frontend environment configuration
# Copy this to .env.local and update as needed

# Backend API URL (update if backend runs on different port/host)
VITE_API_ROOT=http://localhost:4000

# Optional: Enable demo mode (doesn't require Firebase)
VITE_DEMO_MODE=false
